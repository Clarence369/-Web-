//
// Created by hkh on 19-5-25.
//

#ifndef FIRSTCLIONPRO_TIME_EXAMPLE_H
#define FIRSTCLIONPRO_TIME_EXAMPLE_H

#include <time.h>
#include <unistd.h>
/*//（定时器类型，行为控制）
 * timerfd_create(int clockid,int flags)
 * //设置时钟
 * timerfd_settime(int fd,int flags,..)
 * //获取当前时钟
 * timerfd_gettime(int fd,struct itimerspec *curr_value)
 *
 *
 */
void time_example()
{
    //struct iti
}

#endif //FIRSTCLIONPRO_TIME_EXAMPLE_H
